# 全セット例文見直し実装計画

## 目的

第20セットで実施した例文の見直しを、第1〜19セットにも展開する。各語の主例文は次の優先順位で選ぶ。

1. 添付資料の例文
2. 和歌
3. 古典の文章
4. 生成した学習用作例

この計画書では、例文の出典・本文・現代語訳・空欄をセット単位で確認し、確認できない出典を作らないことを完了条件にする。

## 現状監査（2026-08-30・開始時）

- `data/manifest.json` は20セット、各12語、合計240語を登録している。
- 第20セットは今回の先行実施分で、12語中10語が古典本文、2語が和歌。`source: "学習用作例"` は0語。
- 第1〜19セットでは、開始時点で `source: "学習用作例"` が26語あった。これは優先調査の目印であり、作例でない出典欄も本文照合が済んだことを意味しない。

| セット | 和歌 | 古典本文 | 学習用作例 | 備考 |
| ---: | ---: | ---: | ---: | --- |
| 1 | 0 | 12 | 0 | 出典監査 |
| 2 | 0 | 12 | 0 | 出典監査 |
| 3 | 2 | 10 | 0 | 和歌・本文の照合 |
| 4 | 2 | 10 | 0 | 作例1語を本文へ更新済み |
| 5 | 0 | 10 | 2 | 作例2語を維持 |
| 6 | 0 | 12 | 0 | 出典監査 |
| 7 | 1 | 11 | 0 | 和歌・本文の照合 |
| 8 | 1 | 11 | 0 | 和歌・本文の照合 |
| 9 | 1 | 11 | 0 | 和歌・本文の照合 |
| 10 | 1 | 11 | 0 | 和歌・本文の照合 |
| 11 | 0 | 12 | 0 | 出典監査 |
| 12 | 4 | 8 | 0 | 和歌・本文の照合 |
| 13 | 0 | 12 | 0 | 出典監査 |
| 14 | 1 | 11 | 0 | 和歌・本文の照合 |
| 15 | 0 | 12 | 0 | 出典監査 |
| 16 | 0 | 12 | 0 | 作例1語を本文へ更新済み |
| 17 | 0 | 12 | 0 | 作例12語を本文へ更新済み |
| 18 | 2 | 10 | 0 | 作例6語を更新済み |
| 19 | 0 | 12 | 0 | 作例9語を本文へ更新済み |

## 実装進捗（2026-08-31）

- 第19セット: 9語を、NDLで本文確認した古典の文章へ更新済み。`学習用作例` は0語。
- 第16セット: 1語を本文へ更新済み。追加で11語の採用箇所と出典帖名を確認。本文監査の未確認語はなくなった。`学習用作例` は0語。
- 第18セット: 6語を本文・和歌へ更新済み。`学習用作例` は0語。
- 第17セット: 12語を本文へ更新済み。`学習用作例` は0語。
- 第4セット: 1語を本文へ更新済み。`学習用作例` は0語になった。
- 第5セット: 4語を本文へ更新済み。`学習用作例` は0語。
- 第1セット: 10語を本文・出典表記へ反映し、2語の出典を巻・話まで具体化。未確認語はなくなった。
- 第2セット: 8語を本文・訳・空欄へ、3語を出典へ、1語を古今和歌集の和歌へ反映。未確認語はなくなった。
- 第3セット: 和歌2語と詞書1語に加え、散文5語を本文・訳・空欄へ、4語を帖名へ反映。未確認語はなくなった。
- 第6セット: 8語を本文・出典・空欄へ、4語の出典を具体化。未確認語はなくなった。
- 第7セット: 9語を本文・出典・空欄へ反映し、2語の出典を具体化。本文監査の未確認語はなくなった。
- 第8セット: 11語を本文・訳・空欄・出典へ反映。和歌1語・詞書1語・源氏物語1語を照合済み、未確認語はなくなった。
- 第9セット: 11語を本文・訳・空欄・出典へ反映。和歌1語も照合済み。`kv09-103`はNDL確認済みの本文へ更新した。
- 第10セット: 10語を本文・出典へ反映。和歌1語と毎月抄1語は既存内容を照合済み、未確認語はなくなった。
- 第11セット: 11語を本文・出典へ反映。残りの未確認語はなくなった。
- 第12セット: 3語を本文・訳・空欄・出典へ反映。和歌4語と詞書1語は既存内容を維持し、`kv12-144`の本文も確認済み。未確認語はなくなった。
- 第13セット: 12語を本文・出典へ反映。第13セットの未確認語はなくなった。
- 第14セット: 11語を本文・出典へ反映。和歌1語は既存内容を維持し、本文監査の未確認語と文脈クイズの同義語衝突を解消した。
- 第15セット: 4語の本文・出典を更新。8語はPDM本文で採用箇所と帖名を確認。未確認語はなく、本文の表記差も採用方針に合わせて整理済み。

## 固定する実装方針

- 既存の `example`、`translation`、`source`、`cloze`、`exampleForm` を学習画面の互換フィールドとして維持する。
- 複数候補を残す必要がある語だけ、語レコードの `examples` に `sourceType` を付ける。配列の並びではなく、アプリの優先順位で主例文を選ぶ。
- 和歌は `docs/waka-adoptions.json` を入力表とし、`node scripts/apply-waka.mjs` で反映する。散文は対象JSONを直接更新する。
- 和歌は本文、五句、読み、作者、歌集・巻・歌番号を確認する。散文は作品名・段名または章名・本文範囲・底本を確認する。
- NDL資料は `info` で公開区分と収録範囲を確認し、`search` の結果だけで決めず、必要な箇所は版面画像で照合する。
- 源氏物語は既存方針どおり底本を特定しない。これを他作品へ広げない。
- 添付資料を参照できない間は、内容・出典を推測して「添付資料由来」としない。確認できないものは `学習用作例` として残す。
- 語ID、語順、`dataVersion`、語義、進捗キーを変更しない。語義そのものの修正が必要な場合は、例文見直しとは別の変更として扱う。

## 実施順

### 第1段階：第16〜19セット

開始時は `学習用作例` が21語あり、今回の見直し効果が最も大きい。第19セットの9語、第16セットの1語、第18セットの6語、第17セットの4語を実装したため、第17セットと第18セットの `学習用作例` は0語になった。まず既存の `SET_*_PLAN.md` と `SOURCE_EDITIONS.md` の未確定箇所を確認し、和歌候補と本文候補を分けて調査する。

### 第2段階：第4〜5セット

開始時は `学習用作例` が5語あった。第4セットのkv04-045と、第5セットのkv05-052・kv05-054・kv05-058・kv05-060を古典本文へ更新し、現在は第5セットの `学習用作例` も0語になった。kv05-052は『宇津保物語』、kv05-058は『枕草子』の採用箇所を追加監査で確認した。

### 第3段階：第1〜3・第6〜15セット

明示的な作例がない語も、出典欄・本文・空欄の整合をセット単位で監査する。既存の和歌は本文と歌番号、詞書は和歌扱いにしないことを確認する。

各段階は、複数セットを一度に書き換えず、原則として1セットごとに計画・実装・検証を完了させる。

## 1セットごとの作業手順

1. `docs/SET_NN_FIX_PLAN.md` を新規作成または更新し、対象語、現状の出典、候補、未確認点を列挙する。
2. 対象語のID・語義・注記・例文・空欄を控え、変更範囲を固定する。
3. 添付資料の候補が確認できる場合は最優先で照合する。確認できなければ、和歌、古典本文の順に候補を探す。
4. 採用候補をNDLの資料情報・収録範囲・検索結果・版面で確認する。確認できない候補は実出典として登録しない。
5. `docs/SOURCE_EDITIONS.md` に底本と語ごとの採用箇所を追記する。新しい底本を使う場合は底本一覧にも追加する。
6. 和歌は `docs/waka-adoptions.json` へ入力し、散文は語JSONへ反映する。`example` を変えたら、現代語訳と `cloze` も同時に見直す。
7. 形式検査、セット内の文中四択、人手による語義・訳・空欄の確認を行う。
8. 変更したセットの計画書に、採用理由、未確認事項、検証結果を記録する。

## 検証

セットごとに次を実行する。

```powershell
node scripts/check-set-choices.mjs kobun-set-NN --pairs
node scripts/check-data.mjs
node scripts/check-example-source.mjs
node scripts/check-waka-data.mjs
node scripts/check-waka-choices.mjs
node scripts/check-waka-display.mjs
node scripts/check-context-choices.mjs
node scripts/check-srs.cjs
git diff --check
```

和歌やアプリ側の優先順位を変更した段階では、次も実行する。

```powershell
node --check static/example-source.js
node --check static/mode-vocab.js
node scripts/check-meaning-example-ui.cjs
node scripts/check-vocab-goal-ui.cjs
```

公開前には `.github/workflows/pages.yml` の検査項目と突き合わせ、実ブラウザで対象セットのカード、意味確認、文中問題を確認する。公開後はActionsの成功だけで終わらせず、公開URLのHTML、`data/manifest.json`、対象セットJSONを取得して、セットID・語数・語ID範囲・更新済み例文を確認する。

## 注意点と対策

- 添付資料が再提示された場合は、既存の和歌・本文より先に資料中の例文を再照合する。
- NDLの全文検索は関連度順で、作品名やPIDによる絞り込みが十分に効かないことがある。検索結果の採用前に資料情報と版面を確認する。
- `scripts/apply-waka.mjs` は入力表全体を適用するため、保留中の別セットの変更を意図せず作業ツリーへ戻さないよう、対象差分を確認してから実行する。
- 例文が本文に見つからない場合、無理に出典を付けず、`学習用作例` のままにする。出典欄だけ立派になる現象は、古文でも起きる。

## 完了条件

- 第1〜19セットの全語を、優先順位に従って監査・分類する。
- 採用した和歌・本文は、出典と底本が `SOURCE_EDITIONS.md` に記録されている。
- 未確認の出典を実出典として残さず、必要な語は `学習用作例` と明示する。
- 例文、現代語訳、空欄、例文形式が互いに一致し、既存の語ID・進捗互換性を保つ。
- セット検証、全体検証、ブラウザ確認、公開後のデータ取得確認が完了する。

この計画書の作成時点では、第20セット以外のデータは変更しない。2026-08-30の実装で第4・5・16〜19セットを更新した。次は第1〜3・第6〜15セットを、引き続き1セットごとに計画・実装・検証する。

## 追補: 全37セットの例文品質監査（2026-09-05）

登録済み37セット・444語を対象に、例文・現代語訳・出典・空欄の構造、重複例文、学習用作例23語の文体を横断確認した。今回の修正範囲は、本文または訳に明確な問題があり、安全に訂正できる14語に限定する。

- `kv05-051`: 出典本文として照合できない現代語調の文を、出典を装わない学習用作例へ変更する。
- `kv05-055`・`kv16-191`: 現代語訳の口語的すぎる語を、学習用の落ち着いた表現へ改める。
- `kv06-072`: 呼応表現「さらに〜打消」を「さらに」と誤訳していたため、「まったく〜ない」に直す。
- `kv11-121`・`kv12-134`: 敬語の方向と、人名に付く「ぬし」の解釈が伝わる訳へ直す。
- `kv27-313`・`kv27-319`・`kv27-322`: 現代語調の作例、および古文と現代語説明が連結した添付例文を、語義・活用が一意に分かる学習用作例へ変更する。確認できない『宇津保物語』の出典は残さない。
- `kv29-339`・`kv29-340`: 現代文へ古語だけを差し込んだ作例を、NDLのPDM資料で確認した『源氏物語』夕霧・末摘花の本文へ変更する。
- `kv29-343`・`kv29-345`・`kv32-382`: 作例の現代語調、重言、不自然な主題提示を、簡潔な古文へ整える。

語ID、語順、`meanings`、`dataVersion`、進捗キーは変更しない。今回の確認は教材としての本文・訳の品質監査であり、既存の実出典421語すべてを底本画像から再校合する作業ではない。出典再校合が必要な箇所は、既存の `SOURCE_EDITIONS.md` の確認記録を引き継ぐ。

## 追補: 全37セットの例文再監査（2026-09-05・第2回）

444語の `example` / `translation` / `cloze` / `source` を通し読みし、**底本を再校合せずに判定できる誤り**に限って直した。
反映は再実行可能な `scripts/fix-example-quality.mjs` で行う（既存の整形を崩さないよう値の文字列だけを置換する）。
語ID、語順、`meanings`、`notes`、`dataVersion`、進捗キーは変更していない。

### 本文・訳・空欄（10語）

| 語ID | 直した点 |
| --- | --- |
| `kv07-081` | 本文 `え聞きざりけり` は「ざり」が未然形接続で成立しない。`え聞かざりけり` に訂正し、`cloze` も合わせた。訳の「あれま」も改めた |
| `kv18-205` | 会話の開き鉤括弧が欠落。`「人近く聞かざらむは…` に補い、`cloze` も合わせた |
| `kv17-195` | 同じく開き鉤括弧が欠落。加えて訳に本文にない「ここにいる人々に昔の物語を」が入っていたため、本文どおりの訳へ直した |
| `kv02-021` | `cloze` が活用語尾「ひ」を空欄の外に残していた（`（　）ひける`）。連用形「逢ひ」を一続きで問う形に直した |
| `kv30-354` | 「くどく」は嘆きをくり返し述べる意。「念仏を繰り返し唱え」という訳を語義に合わせた |
| `kv35-410` | 竹取物語の同一文を扱う `kv09-100` と訳が食い違っていた。語義①「思案する」に沿って統一した |
| `kv34-397` | 訳の「すぐに、ひたすらどんどん」の重ねを整理した |
| `kv29-343` | 訳の「ご本人が自分で」の重言を解消した |
| `kv11-121` | 訳に本文にない「者でございます」が入っていたため、本文の係り受けに合わせた |
| `kv05-049` | 語義（天皇のお怒り）と『韓非子』の本文が結びつかなかったため、故事の含意を訳に補った |

### 出典表記（59か所）

学習者に見える `source` に、作業用の情報が残っていた。`AUTHORING_STANDARD.md` §2-1 の「作品名＋（巻・段・章）」に揃えた。
底本・頁・コマの情報は [SOURCE_EDITIONS.md](SOURCE_EDITIONS.md) が正本なので、そちらは変更していない。

- NDLビューアのコマ番号を除去（`十訓抄（canvas100）`、`十訓抄（canvas126）`、`宇治拾遺物語（巻第七・canvas65）`、`伊勢物語（上巻・canvas10）`）。
- 底本の頁番号を除去（`枕草子（p.109）`、`更級日記（新註・p.14）`、`源氏物語（添付資料p.112）` など）。`（添付資料）` の但し書きは、本文一致の底本を確定できていない印なので残した。
- `松浦宮物語（巻第五百二）` は底本『続群書類従』の輯次で作品の巻ではないため、`松浦宮物語` にした。
- `土佐日記（宝永版本）` は版本名なので `土佐日記` にした。
- `韓非子・説難` を `韓非子（説難）` に、段番号を漢数字へ統一（`徒然草（第38段）`→`（第三十八段）` など）。

### 検証

`check-data` / `check-example-source` / `check-waka-data` / `check-waka-choices` / `check-waka-display` /
`check-context-choices` / `check-srs`、および `check-set-choices --pairs` を対象セットで実行し、すべて通過。
`git diff --check` も通過。

### 今回直さず残した点

底本の再校合、または語義・見出し語そのものの変更を伴うため、別作業として残す。

- `kv30-349` の見出し語 `あきれる` は現代語形。古語の歴史的仮名遣いなら `あきる`（下二段）。見出し語の変更は進捗キーに関わるため見送った。
- `kv22-259` は見出し語 `ずちなし` に対し本文が `すぢなし`。`notes` に断りがあるので現状維持とした。
- `kv12-134` の出典 `大鏡（太政大臣伊尹伝）` は、本文（明順のぬし）が『枕草子』の可能性がある。底本照合が要る。
- 空欄前後の手掛かりが乏しい短い例文（`kv19-220`「心ゆくもの。」、`kv10-111`「よきに奏し給へ。」など約20語）。実出典なので、差し替えるには別の本文をNDLで確認する必要がある。
- `kv05-049` の例文は『韓非子』のままで、日本古典での「天皇のお怒り」の用例ではない。訳で補ったが、本文差し替えは未実施。

## 追補: 第38〜50セットの例文監査（2026-09-06）

前回の全セット監査（`fde4a4d`）以降に追加された第38〜50セット156語は、例文監査を通していなかった。
600語全体の `example` / `translation` / `cloze` / `source` を機械検査（歌集出典と `exampleForm` の不一致、
例文の重複、現代語彙の混入、空欄の答えの露出、1文字空欄の一覧）にかけ、そのうえで第38〜50セットの156語を通し読みした。

直したのは、**NDLのインターネット公開資料で本文を確かめられた誤りだけ**（8語）。
反映は再実行可能な `scripts/fix-example-quality-3.mjs` で行い、値の文字列だけを置換した。
語ID、語順、`meanings`、`notes`、`dataVersion`、進捗キーは変更していない。

### 本文・出典（8語）

| 語ID | 直した点 | 確認資料 |
| --- | --- | --- |
| `kv41-492` `kv42-494` `kv42-495` | 本文 `ある人が、` は現代語の主格「が」。底本は `ある人の、`。あわせて**出典が『十訓抄』ではなく『徒然草』第二十一段**（「よろづの事は、月見るにこそ慰むものなれ」に続く一文）であることを確認し、`source` を `徒然草（第二十一段）` に直した | 『刪定徒然草』[888777](https://dl.ndl.go.jp/pid/888777) |
| `kv40-473` | `小さくかいらしくして` の「かいらし」は近世語で平安の本文に現れない。『源氏物語』若菜下の女三宮の描写どおり `小さくうつくしげにて` に直した | 『源氏物語湖月抄』[877467](https://dl.ndl.go.jp/pid/877467)、『国文大観』２ [991354](https://dl.ndl.go.jp/pid/991354) |
| `kv47-562` | `女同士も` は現代語。能因本系の本文どおり `女どちも` に直した | 『群書類従』第拾八輯 [1879825](https://dl.ndl.go.jp/pid/1879825) |
| `kv48-571` | `「髭切が、あらぬ太刀が、正直に申さるべし」` では文が成立しない。並列の疑問「か」で、現代語訳もすでに「髭切か、他の太刀か」と訳していた。本文を `「髭切か、あらぬ太刀か、` に直した | 『故実叢書』武家名目抄 [771995](https://dl.ndl.go.jp/pid/771995) |
| `kv49-584` | `おわびを言へど` は現代語。底本は `をこたりを言へど`（＝謝罪）。訳も「詫び言を言うけれど」に直した | 『国文大観』３ [991355](https://dl.ndl.go.jp/pid/991355) |
| `kv43-515` | 例文中の補足だけが片仮名（`（ツバメは）`）だった。他の21か所と同じく `（燕は）` に揃えた | — |

`kv42-494`・`kv42-495` の訳にあった「知的でおもしろい」は、本文にない補いなので `kv41-492` と同じく「（知的で）おもしろい」に揃えた。

### 検証

`check-data` / `check-example-source` / `check-waka-data` / `check-waka-choices` / `check-waka-display` /
`check-context-choices` / `check-srs`、および変更のあった第40〜43・47〜49セットで `check-set-choices --pairs` を実行し、すべて通過。
`git diff --check` も通過。

### 今回直さず残した点

- **`kv44-520`・`kv46-549` は和歌だが `exampleForm` は `"prose"` のまま。** 本計画書の第44・46セットの記録どおり、
  `waka` 構造が要求する作者・歌番号を添付資料が持たないための既定の判断であり、覆していない。
  歌番号を補うには底本の版面照合が要る（`kv46-549` は『拾遺和歌集』下 [873507](https://dl.ndl.go.jp/pid/873507) で
  紀貫之の哀傷歌であることまで確認したが、この版は歌番号を印刷していない）。
- `kv38-447` の見出し語 `かまくて` は、古語辞典の見出し `かまへて（構へて）` と食い違う。
  見出し語の変更は進捗キーに関わるため、例文監査では扱わない。
- 同じ一文を複数語で共有する組（`kv41-482`/`kv41-483`、`kv41-492`/`kv42-494`/`kv42-495` など）は、
  添付資料が一文に複数の傍線を引いている箇所で、空欄位置だけを変えて採っている。意図した運用なので変更しない。
- `kv48-568` の空欄の答え「手」は、同じ例文の「磐手の野辺」にも字として現れる。地名の一部で語義の手掛かりにはならないため、
  1文字空欄の例外として残した。

## 追補: 学習用作例を実出典へ置き換えられるかの点検（2026-09-06）

`source: "学習用作例"` の**59語**を対象に、NDLデジタルコレクションの全文検索で
「その語義・その活用形で使われている本文」を探せるかを1語ずつ当たった。
本文と作品を特定できた**13語**を古典本文へ差し替え、残る46語は理由を付けて据え置いた。
反映は再実行可能な `scripts/replace-generated-examples.mjs` で行う（置換前の値を全文一致で照合してから、
`example` / `translation` / `cloze` / `source` の4フィールドだけを置換する）。
語ID、語順、`headword`、`kanji`、`meanings`、`notes`、`dataVersion`、進捗キーは変更していない。

### 実出典へ差し替えた13語

| 語ID | 見出し語 | 差し替え後の出典 | 本文を確認した資料 |
| --- | --- | --- | --- |
| `kv23-268` | なのめならず | 平家物語（瀬尾最期） | 『平家物語.下卷』[1176917](https://dl.ndl.go.jp/pid/1176917) |
| `kv23-273` | そこら | 源氏物語（桐壺） | 『源氏物語』［1］[2591006](https://dl.ndl.go.jp/pid/2591006) |
| `kv27-319` | あし | 竹取物語 | 『竹取物語』[1096853](https://dl.ndl.go.jp/pid/1096853) |
| `kv28-326` | おとなし | 徒然草（丹波の出雲の段） | 『徒然草』2巻 [2544702](https://dl.ndl.go.jp/pid/2544702) |
| `kv29-347` | いな | 竹取物語（龍の首の玉） | 『竹取物語』[877529](https://dl.ndl.go.jp/pid/877529) |
| `kv31-366` | やつす | 源氏物語（夕顔） | 『源氏物語』［4］[2579512](https://dl.ndl.go.jp/pid/2579512) |
| `kv33-393` | ふるさと | 伊勢物語（初冠） | 『国文大観』３ [991355](https://dl.ndl.go.jp/pid/991355) |
| `kv40-470` | いと | 竹取物語 | 『新制小学国語読本出典文抄』[1438621](https://dl.ndl.go.jp/pid/1438621) |
| `kv40-476` | いざ | 宇治拾遺物語（巻第一・児のそら寝） | 『宇治拾遺物語』[977875](https://dl.ndl.go.jp/pid/977875) |
| `kv47-558` | だいじ | 徒然草 | 『教科新抄徒然草』[1031171](https://dl.ndl.go.jp/pid/1031171) |
| `kv49-587` | きは | 源氏物語（桐壺） | 『源氏物語』［1］[2567038](https://dl.ndl.go.jp/pid/2567038) |
| `kv49-588` | しな | 源氏物語（帚木） | 『源氏物語講本』[947711](https://dl.ndl.go.jp/pid/947711) |
| `kv50-598` | よし | 竹取物語 | 『竹取物語 : 校訂標註』[877627](https://dl.ndl.go.jp/pid/877627) |

差し替えで文中四択の危険が増えていないことも確認した。`kv23-268` はむしろ改善している
（旧作例「姫君の嘆きたまふこと、（　）。」には同セットの「みそかなり」がそのまま入るが、
新本文「倉光の三郎（　）喜び」には活用形が合わない）。`kv49-587` きは と `kv49-588` しな は
語義重複ガードが同時出題を止めているため、双方が身分の意味でも選択肢に並ばない。

### 据え置いた46語とその理由

- **語義が本文と噛み合わない（差し替えると訳が語義から離れる）**: `kv43-510` あぢきなし は
  『源氏物語』桐壺「やうやう天の下にもあぢきなう」を確認したが、そこでの意味は「不都合だ」で、
  登録語義「つまらない。おもしろくない。」と一致しない。
- **和歌になってしまう**: `kv35-414` なる は『伊勢物語』東下り「唐衣きつつなれにしつましあれば…」が
  最有力だが、採ると `exampleForm: "prose"` の和歌がもう1件増える（`kv44-520`・`kv46-549` と同じ問題）。
  歌番号・作者を扱う方針を決めてから手を付ける。
- **句型・語法そのものが見出し語**: `kv05-051` 叡＋〇〇、`kv40-479` いざさせたまへ、`kv48-570` ありありて、
  `kv50-599` よしあり など。実出典を当てると空欄の一意性が崩れるため、`AUTHORING_STANDARD` §1 の方針どおり作例で残す。
- **語が一般的すぎて検索が絞れない**: `kv48-569` あり、`kv48-574` す、`kv47-556` こと（言）、`kv50-590` こころ、
  `kv50-597` ゆゑ、`kv29-343` みづから など。全文検索が他書に押し出され、作品を特定できない。
- **未着手（候補はあるが本文・作品を確定できていない）**: `kv23-270` みそかなり、`kv26-306` ことわり、
  `kv27-313` むげなり、`kv27-321` よろし、`kv27-322` よし、`kv29-345` こころづから、`kv30-353` おきて、
  `kv32-382` つつむ、`kv33-396` さすがなり、`kv34-399` なかなかなり、`kv34-405` おこたり、`kv34-407` おどろかす、
  `kv36-430` あるじまうけ、`kv37-443` たのもし、`kv39-463` かまし、`kv41-484` きよし、`kv41-487` なまめかし、
  `kv41-490` えんなり、`kv42-493` あはれ、`kv42-496` うし、`kv43-507` らうがはし、`kv44-517` むくつけし、
  `kv44-519` あかず、`kv44-524` あさむ、`kv45-535` くもゐ、`kv45-539` きこえ、`kv46-548` むすぶ（結ぶ）、
  `kv47-559` さた、`kv47-560` しるし（徴・験）、`kv48-566`・`kv48-567` つま、`kv49-582` おとづる、
  `kv49-586` かたち、`kv50-592` こころなし。
  検索の当たりはあるものの（例: `kv43-507` らうがはし は『源氏物語』明石の火事の場面、
  `kv34-405` おこたり は『堤中納言物語』「をこたりを言へど」）、版面での本文確定か帖・段の特定が要るため、
  この回では実出典として登録しなかった。**出典欄だけ立派にしないという方針を優先する。**

### 検証

`check-data` / `check-example-source` / `check-waka-data` / `check-waka-choices` / `check-waka-display` /
`check-context-choices` / `check-srs` / `check-meaning-example-ui` / `check-vocab-goal-ui`、および
変更のあった第23・27・28・29・31・33・40・47・49・50セットで `check-set-choices --pairs` を実行し、すべて通過。
`git diff --check` も通過。

## 追補: 作例を和歌へ置き換えられるかの点検（2026-09-06・第2回）

`AUTHORING_STANDARD` §0-1 の採用順（添付資料＞和歌＞古典本文＞作例）に従い、
残る `学習用作例` 46語のうち**和歌に向く語**（心情・自然・恋・無常・時間・人称語）を洗い出し、
和歌へ置き換えられるかを調べた。**2語を和歌へ差し替えた。**
入力は `docs/waka-adoptions.json` に書き、`node scripts/apply-waka.mjs` で反映している。

### 和歌へ差し替えた2語

| 語ID | 見出し語 | 和歌 | 出典 |
| --- | --- | --- | --- |
| `kv47-560` | しるし（徴・験） | しるしなき物を思はずは一坏の濁れる酒を飲むべくあるらし | 万葉集 巻三・338（大伴旅人） |
| `kv34-399` | なかなかなり | なかなかに人とあらずは酒壺になりにてしかも酒に染みなむ | 万葉集 巻三・343（大伴旅人） |

いずれも太宰帥大伴卿「酒を讃むる歌十三首」。本文・作者・巻は底本『万葉集古義』二（PID 1883783）の系統で、
**歌番号は歌番号を印刷しているPDM資料で確認した**（『新定万葉集』上 [1127423](https://dl.ndl.go.jp/pid/1127423) が
「三三八 驗なき物を…」「三四三 なかなかに人とあらずは…」と番号付きで示す。
『上代文学選』[1457484](https://dl.ndl.go.jp/pid/1457484) も同じ並びを示し、既存 `kv10-116` の巻三・337
「憶良らは今は罷らむ」の直後が338であることと一致する）。

### 和歌にできなかった理由 — 歌番号が確認できない

**この回の律速は本文ではなく `waka.ref.number` である。** 次の歌は本文・作者・歌集まで確認できたが、
`AUTHORING_STANDARD` §5 が必須とする歌番号を、底本でもNDL全文検索でも確認できなかったため採用を見送った。

| 語ID | 見出し語 | 候補歌 | 状況 |
| --- | --- | --- | --- |
| `kv50-592` | こころなし | 心なき身にもあはれは知られけり鴫立つ沢の秋の夕暮（西行法師） | 本文は多数のPDM資料で確認。新古今和歌集 巻四・秋歌上とされるが、番号を印刷した資料に当たらない |
| `kv35-414` | なる | 唐衣きつつなれにしつましあればはるばるきぬる旅をしぞ思ふ（在原業平） | 本文は『伊勢物語』（PID 1109206）で確認。古今和歌集 巻九・羇旅歌の番号が確認できない |
| `kv42-496` | うし | 世の中の憂きたびごとに身を投げば深き谷こそ浅くなりなめ | 本文は『源氏物語』注の引用（古今和歌集とする）で確認。巻・部立・番号が確認できない |
| `kv46-549` | むすぶ（掬ぶ） | 手にむすぶ水にやどれる月影のあるかなきかの世にこそありけれ | 既存の `prose` 収録。『拾遺和歌集』下（PID 873507）で**作者が紀貫之**、哀傷の配列であることまで確認したが、この版は歌番号を印刷していない |
| `kv44-520` | あかなくに | あかなくにまだきも月の隠るるか山の端逃げて入れずもあらなむ | 既存の `prose` 収録。古今和歌集にも入るが番号が確認できない |

- 底本として採っている『万葉集古義』『和歌三代集』『拾遺和歌集』下などの明治〜昭和の版は、
  **本文と作者は載せるが通し番号を印刷していない**（`万葉集古義』二 canvas235、『拾遺和歌集』下 canvas73 の版面で確認）。
  万葉集だけは番号付きのPDM資料が別にあるため、今回の2語を確定できた。
- 勅撰集で同じことをするには、番号を印刷したPDM版（または番号付き索引）を底本一覧に足す必要がある。
  **番号の出所を決めるのは方針判断なので、この回では推測で埋めなかった。**

### 和歌に向かないと判断した語

`kv41-487` なまめかし、`kv43-507` らうがはし、`kv44-517` むくつけし、`kv36-430` あるじまうけ、
`kv30-353` おきて、`kv26-306` ことわり、`kv23-270` みそかなり、`kv49-586` かたち などは散文語で、
`AUTHORING_STANDARD` §1 の「敬語・待遇表現・会話特有語は散文」「和歌を無理に増やさない」に従い対象外とした。
`kv44-519` あかず は『万葉集』巻一・37「見れど飽かぬ吉野の川の常滑の…」が有力だが、
空欄が見出し語「あかず」と一致せず（本文は「飽かぬ」）、§4の空欄規則に合わないため見送った。

### 検証

`check-data` / `check-example-source` / `check-waka-data` / `check-waka-choices` / `check-waka-display` /
`check-context-choices` / `check-srs` / `check-meaning-example-ui` / `check-vocab-goal-ui`、および
第34・47セットで `check-set-choices --pairs` を実行し、すべて通過。`git diff --check` も通過。

なお `scripts/apply-waka.mjs` は対象ファイルを再整形するため、`data/set-18.json` の `waka.ref` が
1行表記から他セットと同じ複数行表記に揃った。データ内容の変更ではない。

### 付随して直した入力表・スクリプトの不整合

`node scripts/apply-waka.mjs` を実行したところ、**入力表が古く、既存の修正を巻き戻した**。
再実行しても同じ結果になるよう、入力表とスクリプトを現行データに合わせた。データの意味は変えていない。

- `docs/waka-adoptions.json` の11語・18フィールドが、その後の監査（`0554dcd` / `fde4a4d` など）で
  データJSON側だけ直された値のままだった。`kv03-031`・`kv04-045`・`kv05-049`・`kv05-052`・`kv05-054`・
  `kv05-058`・`kv05-060`・`kv08-086`・`kv12-134`・`kv12-139`・`kv12-144` の `example` / `translation` /
  `source` / `cloze` を現行値へ更新した。これを直さないと、和歌を1首足すたびに
  `源氏物語（藤原の君）`→`学習用作例` のような巻き戻しが混入する。
- `scripts/apply-waka.mjs` の `notesOneLine` 一覧に `data/set-09.json` と `data/set-10.json` が
  抜けていたため、実行のたびに両ファイルの `notes` が複数行へ展開されていた。一覧に追加した。
- `data/set-18.json` の `waka.ref` だけが1行表記で、スクリプトの出力（複数行）と食い違っていた。
  スクリプトに `ref` の整形規則はないため、他セットと同じ複数行表記に揃えた。

**入力表は実行前に現行データと突き合わせる。** 差分を取ってから `apply-waka.mjs` を走らせること。

## 追補: 歌番号を任意にし、和歌を4首追加（2026-09-06・第3回）

前回の点検で、和歌へ置き換えられない理由が**本文ではなく `waka.ref.number`**だったため、
利用者の判断で**歌番号を任意**にした。これに伴い、番号が確認できない歌も採れるようになった。

### 契約の変更

`AUTHORING_STANDARD` §5 の `ref.number` を必須から任意へ変更し、検査と表示を合わせた。

| 変更した箇所 | 内容 |
| --- | --- |
| `docs/AUTHORING_STANDARD.md` §5 | `ref.number` は任意。底本が通し番号を印刷している場合だけ入れる。**推測で埋めない** |
| `scripts/check-data.mjs` | 番号がある場合だけ正の整数であることを検査する |
| `scripts/check-waka-data.mjs` | 同上 |
| `static/example-source.js` | 例文候補の妥当性判定から番号必須をはずす |
| `static/mode-vocab.js` | `wakaRefText` は番号があれば `巻・N番`、なければ `巻` だけを出す |

底本にしている『和歌三代集』『万葉集古義』『拾遺和歌集』下などは、本文と作者は載せても通し番号を印刷していない。
番号を必須にしたままでは、本文・作者・巻・部立まで確認できた歌を採れなかった。

### 和歌にした4語

| 語ID | 見出し語 | 和歌 | 出典 | 確認 |
| --- | --- | --- | --- | --- |
| `kv50-592` | こころなし | 心なき身にもあはれは知られけり鴫立つ沢の秋の夕暮（西行法師） | 新古今和歌集 巻四・秋歌上 | 巻・部立を『日本文学通覧』[872012](https://dl.ndl.go.jp/pid/872012) で確認 |
| `kv35-414` | なる | 唐衣きつつなれにしつましあればはるばるきぬる旅をしぞ思ふ（在原業平朝臣） | 古今和歌集 巻九・羇旅歌 | 底本『和歌三代集』上 [874716](https://dl.ndl.go.jp/pid/874716) canvas36 の版面で本文・作者・巻を確認 |
| `kv44-520` | あかなくに | あかなくにまだきも月の隠るるか山の端逃げて入れずもあらなむ（在原業平朝臣） | 古今和歌集 巻十七・雑歌上 | 同底本 canvas58 の版面で確認 |
| `kv46-549` | むすぶ（掬ぶ） | 手にむすぶ水に宿れる月影のあるかなきかの世にこそありけれ（紀貫之） | 拾遺和歌集 巻二十・哀傷 | 『拾遺和歌集』下 [873507](https://dl.ndl.go.jp/pid/873507) で本文と作者、哀傷の配列を確認 |

- `kv44-520` と `kv46-549` は、**和歌なのに `exampleForm: "prose"` で収録していた既知の2語**。今回そろって和歌形式になった。
  `kv44-520` は歌集を出典にする必要があるため、`source` を `伊勢物語` から `古今和歌集` へ変更した（同じ歌が伊勢物語第八十二段にも入る）。
- 4首とも句のモーラ（5/7/5/7/7 ±1）、`phrases.join("")` と本文の一致、空欄が句をまたがないことを検査で確認済み。
- これで和歌は18語から**24語**になった。

### 今回も見送った語

- `kv42-496` うし: 候補「世の中の憂きたびごとに身を投げば深き谷こそ浅くなりなめ」は古今和歌集の**誹諧歌**で、
  学習例文としての調子が語義（つらい）の説明に向かない。巻・部立も確認できていないため見送った。
- そのほかの `学習用作例` は散文語か句型で、`AUTHORING_STANDARD` §1 の「和歌を無理に増やさない」に従い対象外のまま。

### 検証

`check-data` / `check-example-source` / `check-waka-data` / `check-waka-choices` / `check-waka-display` /
`check-context-choices` / `check-srs` / `check-meaning-example-ui` / `check-vocab-goal-ui`、
第35・44・46・50セットの `check-set-choices --pairs`、`node --check static/example-source.js` /
`node --check static/mode-vocab.js`、`git diff --check` をすべて通過。
適用前後を全600語で突き合わせ、変わったのが `kv35-414`・`kv44-520`・`kv46-549`・`kv50-592` の4語だけであることも確認した。

## 追補: ルーブリック運用後の初回監査（2026-09-07）

`docs/EXAMPLE_QUALITY_RUBRIC.md` の必須ゲート・8観点で全600語を機械的に走査し、
本文・訳・台帳だけで判定できる型（§6）を当てた。
判定を出したあと、同日中に指摘3件をすべて §8 の手順で反映した（下の「反映」節）。

### 走査でかけた検査（全600語）

| 検査 | 結果 |
| --- | --- |
| `cloze` を戻して `example` が復元されるか | 600語すべて復元可（G5 の構造違反なし） |
| 呼応副詞（さらに・つゆ・をさをさ・たえて・いとど・あへて）と訳の対応 | 1語で不一致（下記1） |
| 同一本文を共有する語どうしの訳の突き合わせ | 7組15語のうち1組で不一致（下記2） |
| 1文字空欄29語の答えの露出 | 4語で露出。`kv48-568` は既知の例外、残る `kv02-020`（見／見え）・`kv16-181`（さ／壓され）・`kv17-194`（と／ありとも）は送り仮名・助詞での偶発一致で語義の手掛かりにならない。据え置く |
| 旧字体・踊り字・鉤括弧の対応 | 10語で正規化漏れ（下記3） |
| 現代語混入（です・ます、主格「が」、現代接続詞） | 実質ヒットなし。検出はすべて古典の準体・連体格の「が」 |

### 1. `kv01-001 ありく`: 出典の章段が別の章段を指している（G1 / C）

`source` は `竹取物語（蓬莱の玉の枝）`、台帳（`SOURCE_EDITIONS.md` L140・L725）も「蓬莱の玉の枝（p.34）」だが、
底本『竹取物語 : 校註』（NDL PID [1052421](https://dl.ndl.go.jp/pid/1052421)）の収録範囲は
**蓬莱の玉の枝＝p.13〜22、龍の首の珠＝p.30〜38**。p.34 は龍の首の珠に当たる。

canvas24（p.34/35）の版面で本文を確認した。大伴大納言の段で、

> 「我が弓の力は、龍あらばふと射殺して首の玉は取りてむ。遅く来るやつばらを待たじ」とのたまひて、
> 船に乗りて、海ごとにありき給ふに、いとど遠くて、筑紫の方の海に漕ぎ出で給ひぬ。

- **本文は底本と一致する**（`いとど` を含む）。B は問題ない。誤っているのは章段名だけである。
- 同底本を使う他の8語（`kv08-094` `kv09-099` `kv09-100` `kv09-101` `kv09-107` `kv09-108` `kv14-163` `kv14-166`）は、
  頁と章段の範囲が食い違わない。**章段の取り違えはこの1語だけ**である。
- 併せて訳に2点。`いとど遠くて` を「とても遠い」と訳しており、`いとど`（ますます）の語義が落ちている（C）。
  例文が引用の受け `とのたまひて` から始まるのに、訳がその部分を訳していない（C / E）。

**修正方針**: `source` を `竹取物語（龍の首の珠）` へ、台帳L140・L725の章段を同じく訂正。
訳は「…とおっしゃって、船に乗って、あちこちの海を移動なさるうちに、いっそう遠くなって、筑紫の方の海に漕ぎ出しなさった。」の線で直す。
証拠段階は版面照合済みなので **E3（照合済み）**。

### 2. `kv33-392 ついで` と `kv02-015 せうそく・せうそこ`: 同一本文で訳と `source` が揃っていない（C / D）

両語は `はかなきついで作り出でて、消息など遣はしたりき。` を共有する（空欄位置は別で、役割分担は妥当）。

| 語ID | `source` | 台帳の底本欄 | 証拠段階 | 訳 |
| --- | --- | --- | --- | --- |
| `kv02-015` | 源氏物語（**夕顔**） | 『国文大観.1』PID 991353 夕顔 canvas31 で本文確認 | E2 | …手紙などを**お送りしていました**。 |
| `kv33-392` | 源氏物語 | —（**添付資料 p.186** のみ・底本未確定） | E1 | …手紙などを**おくった**。 |

同じ一文に対して、帖名の有無・敬語の有無・文体が食い違っている。§6 の
「同じ箇所を採った語どうしで訳が食い違う」型で、自動検査では拾えない。

**修正方針**: `kv33-392` の `source` を `源氏物語（夕顔）` へ具体化し、訳を `kv02-015` と一本化する。
台帳L499・L1079を canvas31 の確認済み行に付け替えれば、`kv33-392` は **E1 → E2 へ昇格**できる。
どちらの訳文へ寄せるか（`遣はす` を尊敬として訳すか）は、canvas31 の話者を確認してから決める。**現時点では未確認**。

### 3. 底本の字体・記号の正規化漏れ 10語（B / §6「記号の欠落・表記ゆれ」）

台帳L725 が「本文では旧字体を新字体に改め」と方針を明記しているが、次が未適用のまま残っている。

| 種別 | 語ID | 該当 |
| --- | --- | --- |
| 旧字体 | `kv03-030` | 數へて → 数へて |
| 旧字体 | `kv05-058` | 大貳 → 大弐 |
| 旧字体 | `kv08-085` | 鳥の聲 → 声、氣色 → 気色 |
| 旧字体 | `kv11-130` | 二條殿・歸らせ → 二条殿・帰らせ |
| 旧字体 | `kv11-132` | 條 → 条 |
| 旧字体 | `kv15-170` | 思し亂れ → 乱れ |
| 旧字体 | `kv16-181` | 壓され → 圧され |
| 旧字体 | `kv16-188` | 參り・中將 → 参り・中将 |
| 旧字体 | `kv19-218` | 蟲の音 → 虫の音 |
| 踊り字 | `kv08-085` | 心あわた**ゞ**しく → あわただしく |
| 鉤括弧 | `kv20-232` | 開き「がなく `菩薩や」` だけが閉じている（`kv18-205`・`kv17-195` と同型） |

学習者に見える本文なので、同じ作品・同じ底本から採った他の語と字体が揃っていないと、
どこまでが原文でどこからが正規化か分からなくなる。`kv16-181` は1文字空欄（答え「さ」）が
`壓され` の送り仮名と一致しており、新字体化すると露出も同時に解消する。

**修正方針**: `AUTHORING_STANDARD` の正規化方針に沿って、再実行可能なスクリプトで置換する。
`example` を変えたら `cloze` と `examples` 候補側の同じ値もまとめて直す（`kv08-085` は90字超の既知9語でもある）。

### 実行した検査

`node scripts/report-example-stats.mjs`、`node scripts/check-set-choices.mjs kobun-set-41 --pairs`、
`node scripts/ndl.mjs info 1052421`、`node scripts/ndl.mjs image 1052421 24`（版面照合）。
`kv41-482 きよらなり` / `kv41-483 きよげなり` は同一本文・同一セットで G6 が疑われたが、
`static/meaning-guard.js` が両語の同時出題を抑止しているため別解は成立しない。**据え置く。**

### 据え置いた既知の保留（再掲・変更なし）

ルーブリック §11 の6項目（底本未確定144語、台帳未記載11語、学習用作例42語、90字超9語、
14字以下46語、`kv05-049`・`kv12-134`）は今回も未着手のまま。

### 反映（2026-09-07・同日）

`node scripts/fix-example-quality-4.mjs` で、値の文字列だけを raw テキスト上で置換した。
JSON を作り直していないので既存の整形（1要素配列のインライン表記）は崩れていない。
スクリプトは冪等で、再実行すると「変更なし（適用済み）」になる。

| 指摘 | 反映内容 | 対象 |
| --- | --- | --- |
| 1 | `source` を `竹取物語（蓬莱の玉の枝）` → `竹取物語（龍の首の珠）`。訳を「…とおっしゃって、船に乗って、あちこちの海を移動なさるうちに、**いっそう遠くなって**、筑紫の方の海に漕ぎ出しなさった。」へ改め、未訳だった `とのたまひて` と `いとど` を回復した | `kv01-001` |
| 2 | `kv33-392` の `source` を `源氏物語（夕顔）` へ具体化（主例文と `examples` 候補の両方）。`kv02-015` の訳を「手紙などを**おくった**」へ揃えた。夕顔canvas31の版面で、この一文が惟光の詞中にあり `遣したりき` に尊敬・丁寧の標識がないことを確認し、本文にない敬意を足していた旧訳を落とした | `kv02-015` `kv33-392` |
| 3 | 旧字体15字の変換表で `example` と `cloze` を正規化（11語）。踊り字「あわた**ゞ**しく」を展開（1件）。`kv20-232` の欠けていた開き「」を補った（1件） | `kv03-030` `kv05-058` `kv08-085` `kv11-130` `kv11-132` `kv12-136` `kv15-170` `kv16-181` `kv16-188` `kv16-191` `kv19-218` `kv20-232` |

初回監査では旧字体を9語と数えたが、変換表を全例文の使用漢字822字から作り直したところ
`kv12-136`（櫻）と `kv16-191`（燒）が追加で見つかり、**11語**になった。
`kv16-188` の `辨`→`弁`、`kv11-132` の `錄`→`録` も同じ拡張で拾えた。

底本の字面そのものは各語の確認記録のとおりで、変えたのは学習画面へ出す表記だけである。
`kv16-181` は1文字空欄の答え「さ」が「壓され」の送り仮名と字として一致していたが、
新字体化（圧され）で露出も同時に解消した。

**変えていないもの**: `id`・語順・`headword`・`kanji`・`meanings`・`notes`・`meta.dataVersion`・
進捗キー・空欄の答えの文字列。スクリプトはこれらが変わったら例外で止まる。

### 反映後の検証

- 適用前後を全600語で突き合わせ、変わったのが上記**15語だけ**であることを確認した。
  `git diff --stat data/` は 29 insertions / 29 deletions で、書き換えたフィールド数と一致する（整形ノイズなし）。
- ルーブリック §10 の一覧を全て通過。`check-data` / `check-example-source` / `check-waka-data` /
  `check-waka-choices` / `check-waka-display` / `check-context-choices` / `check-srs` /
  `check-meaning-example-ui` / `check-vocab-goal-ui`、`node --check` 3件、`git diff --check`。
- 変更のあった第1・2・3・5・8・11・12・15・16・19・20・33セットで
  `check-set-choices.mjs --pairs` を実行し、全て通過。
- 旧字体・踊り字・鉤括弧の不対応が全600語で**残っていない**ことを再走査で確認した。
- `report-example-stats.mjs` の集計値に変化なし（ルーブリック §2 の実測表は更新不要）。

### 台帳（`SOURCE_EDITIONS.md`）の更新

- L140 `kv01-001` の採用箇所を「龍の首の珠（p.34、canvas24）」へ訂正。L725の本文中の章段名も訂正。
- L499 `kv33-392` の採用箇所を「夕顔（canvas31）」へ。E1 → **E2**。
- 末尾に「2026-09-07 ルーブリック初回監査に伴う訂正」節を追加し、3件の根拠と正規化11語＋2件の一覧を記録した。

### この反映で解消しなかったもの

`kv01-001` は証拠段階 **E3（照合済み）** になったが、ルーブリック §11 の他の保留
（底本未確定144語、台帳未記載11語、学習用作例42語、90字超9語、14字以下46語、
`kv05-049`・`kv12-134`、見出し語側の `kv30-349`・`kv38-447`・`kv22-259`）は**未着手のまま**である。
コミット・push・デプロイは行っていない。

## 追補: 保留の解消 — G1違反11語・kv12-134の底本照合・90字超（2026-09-07・第2回）

ルーブリック §11 の保留のうち、優先度の高い3件を解消した。反映は
`node scripts/fix-example-quality-5.mjs`（再実行可能・冪等、raw テキスト置換で整形を崩さない）。
根拠と語ごとの記録は `SOURCE_EDITIONS.md` の「2026-09-07 保留の解消（第2回）」節にある。

### 1. 台帳に記録のない実出典11語（唯一のゲート違反 G1）

11語すべてを NDL の全文検索で当たり、**10語で本文一致を確認**して台帳へ記録した。
`kv05-055 いへばさらなり` だけは『夜の寝覚』の PDM 資料を索引上で特定できず、
**E1（底本未確定）として記録**した。作品名の根拠が添付資料だけという点で第30〜50セットの144語と同じ状態であり、
§3-1 のとおりゲート違反ではない。実出典を装ったままにはせず、実例と同等にも評価しない。

`report-example-stats.mjs` の集計は次のように動いた。

| 区分 | 前 | 後 |
| --- | ---: | ---: |
| 底本・確認資料あり | 246 | **256** |
| 底本未確定（その他） | 144 | 145 |
| 学習用作例 | 41 | 42 |
| 台帳に未記載 | 32 | **20** |

残る20語は各セットの「本文確認」の地の文に記述があるもので、G1 違反ではない（§2-1）。
**表形式の行が無いだけなので、`report-example-stats.mjs` の「未記載」には出続ける。** 別途の整理課題とする。

### 2. 典拠確認の副産物 — 本文の誤り3件

全文検索でどの資料にも存在しない字面が3件見つかった。底本の字面へ直した。

| 語ID | 誤 → 正 |
| --- | --- |
| `kv05-053` | 汗も**しどに**成りて → 汗も**しとどに**成りて |
| `kv05-059` | **あるものは**角生ひたり → **或は**角生ひたり |
| `kv12-134` | と**宣へりける**にこそ → と**宣ひける**にこそ |

出典も2語具体化した（`kv05-050` → 源氏物語（桐壺）、`kv05-053` → 源氏物語（夕顔））。
どちらも本文自体が場面を特定できる語に限り、**巻・帖・段を推測では埋めていない**。

### 3. `kv12-134` の底本照合 — 枕草子説は **NOT_REPRODUCED**

L204 で「本文（明順のぬし）が『枕草子』の可能性がある」として保留していたが、**枕草子ではなかった。**
「明順のぬし」の全文検索ヒット25件はすべて『大鏡』の諸本で、枕草子の校註本4種のいずれにも当たらない。
『大鏡』（PID 1032685）中巻 p.104・canvas58 の版面で本文と頭注を確認し、この頁が
同書 structures の「太政大臣 伊尹（p.89〜106）」の範囲内にあることも確かめた。
**現行の出典表記「大鏡（太政大臣伊尹伝）」は正しい。証拠段階 E3（照合済み）。**

### 4. 90字超9語の短縮可否（G9）

5語を現行本文の連続した一部分へ短縮し、4語は理由を記録して据え置いた。**90字超は 9語 → 4語。**

| 短縮した | 字数 |
| --- | ---: |
| `kv17-200` いかで | 152 → 39 |
| `kv16-183` さて（しも）ありぬべし | 117 → 60 |
| `kv17-196` とかく | 105 → 60 |
| `kv20-236` さかしら | 108 → 68 |
| `kv16-188` さらぬ | 96 → 48 |

据え置いた4語（`kv05-060` `kv08-085` `kv16-186` `kv16-190`）は、いずれも短縮すると
係り受け・係り結び・逆接の受け・「さればよ」が受ける予想のどれかが切れる。理由は台帳に語ごとに書いた。

短縮は**現行本文の連続した一部分**に限り、省略箇所をつないだ引用は作っていない
（スクリプトが置換前に連続部分であることを検査する）。`translation` も同じ範囲へ切り直した。
`kv17-200` と `kv16-188` は推奨レンジ（15〜40字）まで下がり、E観点が上がる。

### 検証

- 適用前後を全600語で突き合わせ、この2回で変わったのが**23語**だけであることを確認した。
  `git diff --stat data/` は 50 insertions / 50 deletions で書き換えたフィールド数と一致（整形ノイズなし）。
- ルーブリック §10 の一覧を全通過。第5・12・16・17・20セットの `check-set-choices --pairs` も通過。
- 空欄の答えの文字列は23語すべてで不変（スクリプトが検査して、変われば例外で止まる）。

### 新しく見つかった課題（この回では直していない）

- **`kv04-042 さらぬわかれ` は和歌を `prose` で収録している。** 本文
  「老いぬればさらぬ別れのありといへばいよいよ見まくほしき君かな」は五句に切れる和歌で、
  `kv44-520`・`kv46-549` と同じ型（§6「`exampleForm` と実態の不一致」）。
  和歌化には `docs/waka-adoptions.json` ＋ `apply-waka.mjs` の経路と、作者・歌集・巻の確定が要るため別課題とする。
- `kv05-056 さらなり` の帖を特定できていない。底本『源氏物語.下』(942777) は宇治十帖のみを収録し、
  内容（出家後の勤行・法華経・雪深く・年もかへりぬ）から手習の可能性が高いが、**版面で確認していない**。
  `source` は「源氏物語」のままにした。

## 追補: 前回の「新しく見つかった課題」2件の処理（2026-09-07・第3回）

### `kv05-056 さらなり` の帖を特定 — 手習（**解消**）

底本『源氏物語.下』（PID 942777）は宇治十帖だけを収める。structures の手習は p.112〜166（canvas63〜89）。
版面の柱を canvas72（p.130／秋）・canvas76（p.138／「この春初瀬に詣でて」＝年明け後の春）・
canvas84（p.154）の3か所で確認し、いずれも「手習」だった。
採用本文は「雪深く…年もかへりぬ」と冬から年明けを述べるので、**秋と春の間**＝手習の範囲に収まる。
`source` を「源氏物語」→「**源氏物語（手習）**」へ具体化した。反映は `node scripts/fix-example-quality-7.mjs`。

### `kv04-042 さらぬわかれ` の和歌化 — 典拠は確定、**出題側の制約で見送り**

典拠は確定した。『和歌三代集』上 古今和歌集（PID 874716）canvas59（p.95）で巻第十七の柱・詞書・歌本文を、
『古今和歌集 : 校註』（PID 1031921）canvas106（p.184）で巻十七 雑歌上・詞書「業平朝臣の母のみこ」・
頭注「さらぬ別―死別」を確認した。**どちらの底本も通し番号を印刷していない**ので `ref.number` は入れない。

いったん `docs/waka-adoptions.json` へ入れて `apply-waka.mjs` で反映したが、
`scripts/check-waka-choices.mjs` が落ちたため**差し戻した**。

> kobun-set-04/kv04-042: no same-set same-mora safe distractor exists for the priority check

和歌の文中四択は、同セット内に見出し語のモーラ数が±1以内かつ語義の衝突しない誤答語を要求する。
`さらぬわかれ`（6モーラ・語義「死別」）に対し第4セットで同モーラなのは
`kv04-039 むなしくなる`（6）と `kv04-040 いたづらになる`（7）だけで、どちらも語義が「死ぬ」。
語義族ガードに掛かり安全な誤答が作れない。第4セットが死・別れを主題にした構成であることが原因で、
**ガードを緩めて通すことはしない**（ルーブリック §1-1 のとおり、セット構成は出題側の設計の領分）。
和歌化はセット構成の見直しとセットで扱う別課題として残す。現行は `伊勢物語` の散文のまま。

### `docs/waka-adoptions.json` の巻き戻し危険を1件除去

`apply-waka.mjs` は**入力表の全行を適用する**（過去に11語18フィールドが旧値へ巻き戻った経緯がある）。
実行前に51行すべてを現行データと突き合わせたところ、**`kv12-134` と `kv12-136` の
`example`・`cloze` が第2回で直した値より古いまま**だった。そのまま実行すれば
「宣へりける」「櫻の直衣」へ巻き戻っていた。4フィールドを現行データへ同期してから実行した。

**この入力表は今後も同じ罠になる。** 例文を直したら、その語が `waka-adoptions.json` にあるかを確認し、
あれば入力表側も同時に直す必要がある。

### 検証

`check-data` / `check-example-source` / `check-waka-data` / `check-waka-choices` / `check-waka-display` /
`check-context-choices` / `check-srs` / `check-meaning-example-ui` / `check-vocab-goal-ui`、
第4・5セットの `check-set-choices --pairs`、`git diff --check` をすべて通過。
差し戻し後、`kv04-042` が HEAD の値と完全一致することも確認した。

### 作業中に起きたこと（記録）

- `data/set-05.json` の未コミット変更が、利用者の 12:11 のコミット
  `fix(data): 「〇〇といふも世の常なり」の傍線範囲を慣用句へ修正` に一緒に取り込まれた。
  同コミットで `kv05-058` の空欄が「めでたし」から慣用句全体へ移っている。データの欠落はない。
- 切り出した別セッション（`kv05-049`）が**同じ作業ツリー**で動いており、
  `scripts/fix-example-quality-6.mjs` を先に作成した。こちらの `kv05-056` 用スクリプトは
  `fix-example-quality-7.mjs` へ名前を変えた。`kv05-049` は既に太平記の本文へ差し替わっている。

## 追補: `kv05-049 げきりん` の例文差し替え（2026-09-07）

L179・L206 で保留していた「例文が『韓非子』説難の故事本文で、日本古典における
『天皇のお怒り』の用例ではない」件（ルーブリック G3）を、**実例への差し替えで解消した。**
反映は再実行可能な `scripts/fix-example-quality-6.mjs`（raw テキスト上の全文一致置換）。
`id`・語順・`headword`・`kanji`・`meanings`・`notes`・`meta.dataVersion`・進捗キー・空欄の答えは変えていない。

### 差し替えた内容（1語・4フィールド）

| フィールド | 旧 | 新 |
| --- | --- | --- |
| `example` | 人主も亦た逆鱗有り。 | 君大きに逆鱗あつて、この宮を流罪に処すべしとて、中殿の御会に事を寄せ、兵部卿親王をぞ召されける。 |
| `translation` | 君主にもまた、（触れれば激しい怒りを招く）逆鱗がある。 | 帝はたいそうお怒りになって、この宮を流罪に処すべきだといって、中殿の御会（宮中の歌会）にかこつけ、兵部卿親王をお召しになった。 |
| `cloze` | 人主も亦た（　）有り。 | 君大きに（　）あつて、…（以下 `example` と同じ） |
| `source` | 韓非子（説難） | 太平記（兵部卿親王流刑の事） |

空欄の答えは差し替え前後とも「逆鱗」で不変。`exampleForm` は `prose` のまま。

### 典拠の確認

『新訂太平記.第2編』（大町桂月校訂、至誠堂書店、大正1-2年、NDL PID [915963](https://dl.ndl.go.jp/pid/915963)、PDM）
「九八 兵部卿親王流刑の事」p.379・canvas24 の版面で本文を確認した。後醍醐天皇が令旨を証拠として
大塔宮（兵部卿親王）を流罪に処する場面で、「君（＝帝）の逆鱗」が `meanings`「天皇・上皇・法皇のお怒り」と一致する。
旧字体（處・會）は既存の表記方針に合わせて新字体に改めた。証拠段階 **E3（照合済み）**。台帳の語ごとの対応と底本一覧も更新した。

**巻次は書かない。** この底本は節を通し番号（九二〜）で組み、巻の区切りを印刷していない。
『太平記 : 抄註』（[877610](https://dl.ndl.go.jp/pid/877610)）は次節「竜馬進奏の事」を第十三巻とするが、
当該節の所属巻は版面から確定できないため、`AUTHORING_STANDARD` §2-1 の「推測で埋めない」に従い作品名＋節名にとどめた。

### 採らなかった候補（記録）

NDL全文検索（`逆鱗` 500件）を活字翻刻の古典本文へ絞って当たった結果、次も日本古典の用例として成立するが採らなかった。

| 候補 | 本文 | 見送りの理由 |
| --- | --- | --- |
| 『保元物語』巻二・為義最期の事（[1105472](https://dl.ndl.go.jp/pid/1105472) p.93・canvas67 で版面確認） | …様々に両度まで奏聞せられけれども、主上逆鱗あつて、清盛既に叔父を誅す。 | 「主上」が隣接して語義の手掛かりは最も明快だが、続く勅諚の途中で切れる。文として完結させると「清盛既に叔父を誅す」の前提（清盛が叔父忠正を斬った直前の話）まで要る |
| 『保元物語』巻一・主上三条殿行幸の事 | 早く凶徒を追討して、逆鱗を休め奉らば、先づ日来申す所の昇殿に於ては、疑あるべからず | 「逆鱗を休め奉る」は良い共起だが、怒りの主体が本文に現れず、語義（天皇のお怒り）を例文だけで判定しにくい |
| 『源平盛衰記』「法皇逆鱗義仲愁訴の事」（[881148](https://dl.ndl.go.jp/pid/881148)） | （章題） | 章題であって本文の一続きではない |
| 『古今著聞集』『群書類従』所収本ほか | — | ヒットしたのは古典籍の変体仮名本文で、版面から字面を確定できない |

### 検証

- 適用前後を全600語で突き合わせ、**変わったのは `kv05-049` の1語だけ**であることを確認した（`diff` 1行）。
- スクリプトを2回続けて実行し、2回目が「変更なし（適用済み）」になること（再実行で結果が変わらないこと）を確認した。
- ルーブリック §10 の一覧（`check-data` / `check-example-source` / `check-waka-data` / `check-waka-choices` /
  `check-waka-display` / `check-context-choices` / `check-srs` / `check-meaning-example-ui` / `check-vocab-goal-ui` /
  `report-example-stats`、`node --check` 3件）と `check-set-choices.mjs kobun-set-05 --pairs`、`git diff --check` をすべて通過。
- 第5セットの他の見出し語（`勅＋〇〇`・`叡＋〇〇`・`さらなり`・`えもいはず` ほか）を空欄へ入れても
  「君大きに（　）あつて」が成立しないことを人手で確認した（正答一意）。
- 例文長は10字→48字。推奨レンジ（15〜40字）は超えるが通常上限60字以内で、E観点は2点。
  文を「…流罪に処すべしとて、」で切ると述語が落ちるため、一文をそのまま採った。

## 追補: 第30セットの典拠昇格（2026-09-07）

底本未確定（証拠段階 E1）のまま実出典名を表示していた語のうち、方針上の例外である源氏物語（`kv30-350`
`kv30-355` `kv30-356`）と学習用作例（`kv30-353`）を除く7語を対象に、NDLデジタルコレクションの
全文検索と版面で本文照合を行った。台帳の記録は `SOURCE_EDITIONS.md`「第30セットの典拠昇格」にある。

| 語 | 判定 | 証拠段階 | 本文の変更 |
| --- | --- | --- | --- |
| `kv30-349` あきれる | 昇格 | E1 → **E3** | あり（「ここは」→「こは」） |
| `kv30-351` うちとく | 昇格 | E1 → **E2** | あり（「寝るもいとはづかし」→「寝ぬる後もはづかし」） |
| `kv30-352` おきつ | 昇格 | E1 → **E2** | あり（「者どもなり。戦の様をも」→「者なれば、軍のやうをも」） |
| `kv30-354` くどく | 昇格 | E1 → **E2** | なし |
| `kv30-358` ときめく | 昇格 | E1 → **E2** | なし |
| `kv30-359` ときめかす | 昇格 | E1 → **E2** | なし |
| `kv30-360` ながむ | 据え置き | E1 のまま | なし |

### 分かったこと

- **添付資料の本文は、3語で底本と食い違っていた。** 第17セットで11語全てが底本と合わなかったのと同じ型で、
  作品名が正しくても本文がそのまま信用できるとは限らない。`kv30-349` の「ここはいかにせん」は
  指示語「こは（此は）」の重複誤り、`kv30-351` の「寝るもいとはづかし」・`kv30-352` の「者どもなり／戦の様」は
  全文検索でどの資料にも当たらない字面だった。
- **「古い者」は現代語の混入ではない。** `kv30-352` の「汝らは古い」は流布本系『平家物語』の字面そのもので、
  1176917・991360・877646 の3本が一致する。現代語らしく見える形でも、底本に当たってから判断する。
- **踊り字は全文検索で `〓` に化けるため、繰り返し語は当たらないことがある。** `kv30-351` の「言ひ言ひの果ては」は
  「いひいひの…」では0件、「はてはみなうちとけて」で当たった。繰り返しの後ろから引くと見つかる。
- **`structures` に段名が無い版でも、頁番号から段を絞れる。** `kv30-349` は段名を持つ岩波本（PID 1031664）の
  `structures`（御産/129・公卿揃/132）と、他版のOCRに混じった柱「130」を突き合わせて巻第三「御産」に絞り、
  版面で確認した。

### `kv30-360` ながむ を据え置いた理由

『続群書類従.第18輯ノ上 物語部』（PID 936506）巻第五百二・松浦宮物語に
「旅の道とて。戀しかなしとも。心さしもあらじと。つゝむ人めにつゝみかねて。しづかにうちながむるひまもなし。」
があり、対象語の語形・語義（物思いにふける）も一致するため、これが対応箇所と見られる。
一方、現行本文の「若き限りは送りにと慕ひ来て、いと騒がしければ」「心静かにながむるひまなし」は
全文検索でどの資料にも当たらない。canvas21〜23（p.34〜39、弁少将の唐土渡航前後）を版面で読んだが該当丁を
特定できず、差し替え本文の字面を確定できていない。**本文を版面で確かめるまで、底本未確定のまま据え置く。**
次の一手は、936506 の 松浦宮物語 全丁（canvas19〜51）から「つゝむ人めに」の丁を特定し、
その一続きの本文へ差し替えることである。

### 検証

- 適用前後を全600語で突き合わせ、変わったのは `kv30-349` `kv30-351` `kv30-352` `kv30-358` `kv30-359` の
  5語だけであることを確認した。
- `scripts/fix-example-quality-6.mjs` を2回続けて実行し、2回目が「変更なし（適用済み）」になることを確認した。
- ルーブリック §10 の検証コマンド一覧と `check-set-choices.mjs kobun-set-30 --pairs`、`git diff --check` を通過。
- 第30セットの他の見出し語を各 `cloze` へ入れ、変更した3語で別解が成立しないことを人手で確認した。
