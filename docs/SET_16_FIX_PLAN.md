# 第16セット 例文見直し計画・実施記録

## 対象

第16セットのうち、`学習用作例` になっていた `kv16-184 さてしもあらず` を対象にする。添付資料は現在の環境から参照できないため、資料由来とは扱わず、NDLで本文を確認できた古典の文章へ差し替える。

## 採用

- 作品・箇所: 『十六夜日記』冒頭
- 本文: 「また、さてしもあらで、なほこのうれへこそ、やるかたなく悲しけれ。」
- NDL資料: 『十六夜日記讀本』、PID 1083875（PDM）
- 採用理由: `さてしもあらず` の「そのままではいられない」という意味が、直後の「この憂へこそ…悲しけれ」と結びつき、語義を文脈で確認できる。
- 表記: `さてしもあらで` の活用形を本文に残し、空欄は `さてしもあら` として助動詞相当の語尾を手掛かりにする。

## 変更範囲

`data/set-16.json` の `example`、`translation`、`source`、`cloze` のみを更新する。ID、語順、`dataVersion`、語義、注記、進捗キーは維持する。

## 実施結果

2026-08-30に実装した。`kv16-184` は `source: "十六夜日記（冒頭）"` となり、第16セットの `学習用作例` は0語になった。全体データ検査、出典形式検査、文中選択肢検査を実行した。添付資料との同一性は未確認である。
