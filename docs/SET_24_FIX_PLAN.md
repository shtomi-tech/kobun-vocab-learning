# 第24セット 傍線範囲修正計画

## 目的

第24セットの `kv24-277 ほいなし` について、意味確認画面の傍線部を「本意なけれ」全体にする。

## 監査結果

- 現在の `cloze` は「本意」だけを空欄にし、「なけれ」を空欄の後ろへ残している。
- `static/mode-vocab.js` は `cloze` の空欄前後と `example` の共通部分から傍線範囲を算出するため、現在の表示は「本意」だけになる。
- 例文・現代語訳・出典・見出し語・ID・学習進捗キーは変更しない。

## 実施内容

1. `data/set-24.json` の `kv24-277.cloze` を「本意なけれ」全体を空欄にする形へ変更する。
2. 形容詞「なし」の活用形を空欄に含めるため、`scripts/check-data.mjs` に `kv24-277` 限定の理由付き例外を追加する。
3. 空欄を正答で復元した結果が元の例文と一致することを確認する。
4. 既存のデータ検証とUI傍線範囲検証を実行する。

## 完了条件

- `cloze` の空欄を「本意なけれ」で復元すると、元の例文と一致する。
- 傍線範囲の算出結果が `本意なけれ` 全体になる。
- 既存セットを含む関連検証が通る。
